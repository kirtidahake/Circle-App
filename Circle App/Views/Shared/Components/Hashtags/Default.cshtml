@model List<Hashtag>
﻿<div class="flex justify-between text-black">
    <h3 class="font-bold text-base">Trends for you</h3>
</div>

@foreach (var item in Model)
{
<div class="space-y-3.5 capitalize text-xs font-normal mt-5 mb-2 text-gray-600">
    <a>
        <div class="flex items-center gap-3 p">
            <div class="flex-1">
                <h2 class="font-semibold text-black">@item.HashtagName</h2>
                <div class="mt-0.5 mb-2">@item.Count @(item.Count > 1 ?"posts" : "post") </div>
            </div>
        </div>
    </a>
</div>
}
